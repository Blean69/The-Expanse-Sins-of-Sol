# Worker B — isolated torpedo05 appearance

The input is Worker C's actual isolated Amun torpedo at `validation/assets/derived/amun05-c/torpedo-editable/amun05_torpedo.gltf`, with its binary and four4096-square image dependencies. All inputs remain read-only and are hashed in metadata/provenance. No substituted model, reoptimization, unit edit, physics edit or runtime test was performed.

The two indexed subparts contain1534 and66 triangles, totaling1600. Geometry, source normals, UVs and authored tangent directions are retained; no simplification is applied. The narrow red nose points along+Z, verified against C's actual source preview. Length16.51075554 matches C's normalized Javelis-length derivative; dimensions approximately1.681256×1.681261×16.510756. Actual vertex radius8.28733873 is slightly larger than installed stock radius; bounds decisions remain with the main integrator.

Installed `trader_medium_torpedo.mesh` provides observed point names center,above,aura,exhaust.0. The new points use this torpedo's actual bounds. Exhaust is centered on the complete aft-most body slice and offset0.03 outward: approximately[-0.00410667,0.00205332,-8.28537777], up+Y/forward−Z. The side/length normalization does not reuse a larger ship's plume coordinates.

Four1024-square maps are generated separately from the unchanged4096-square atlases for this small projectile. CLR retains source base color/opaque alpha. ORM uses R255 because no AO texture exists; copying source metallic textureR0 would incorrectly make the model fully occluded. G roughness/B metallic follow actual glTF references. MSK R/G/A are zero, B uses the maximum source emissive channel, following the pinned shader's emissive-maskB convention. Game emission is tinted by CLR rather than an independent emissiveRGB texture; this is a documented appearance approximation. Emissive factor1 is used.

The referenced normal texture filename contains DirectX although it is used as a glTF normal. No unsupported green-channel reversal was applied. The normal vectors are renormalized after resize and encoded BC5_SNORM; CLR/ORM/MSK use BC7_UNORM. Inspect bump direction and highlights during the first close view. This naming ambiguity is not hidden as a passed runtime shading test.

Official MeshBuilder JSON and binary outputs are generated separately with facing grids. Source winding is corrected and the official grid regenerated. The established bounded16-byte tangent-field repair leaves every other binary byte, index and official trailer untouched. Geometry position reconstruction, binary/JSON parity, finite orthogonal unit tangent frames, zero opposed winding and actual exhaust point basis pass. Rest position reconstruction error is zero; tangent fallback count is zero.

Final handoff: `integration-spec.json`, `mount-metadata.json`, `output-validation.json` and `final-provenance.json`. Generated game resources are under `build/torpedo05-b/game`: one private mesh, one actual compiler material alias and four DDS maps. Original editable source stays in Worker C's tree; this worker writes only a separate compiler derivative and generated texture sources. The main integrator owns private projectile appearance definitions, any necessary geometric bounds, manifests and installation decisions.

Reproduction order from this worktree with OPENBLAS_NUM_THREADS=1: `torpedo05_prepare.py`; `torpedo05_compile.py --sdk <pinned SDK> --wine <installed Proton wine64>`; `torpedo05_winding.py` with the same arguments; `torpedo05_finish.py`; `torpedo05_textures.py --wine <same wine64>`; `torpedo05_spec.py`. The Wine prefix is exclusively `build/torpedo05-b/proton-prefix`. Missing source/SDK/texconv dependencies must fail rather than download substitutes. No installed files are build targets.

Runtime gates: inspect nose/exhaust orientation, trail attachment and normal-map direction; verify projectiles launch/intercept/impact exactly as the unchanged definitions specify; compare projectile fleet performance with stock. Visual appearance alone does not prove interception. All runtime results for this candidate remain NOT RUN.
